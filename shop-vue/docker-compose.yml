version: '3.9'

services:
  vue:
    container_name: vue-shkaf
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '8081:8080'
